#include <kernel/mem.h>

int main() {
  //memory_map_t memory_maps[3];
  //unsigned map_elements = 3;
  //mobule_binary_t binaries[2];
  //unsigned nof_binaries = 2;
  //  mem_early_initialize(memory_map, map_elements,
                       //                     binaries, nof_binaries);

  //uintptr_t mem_alloc_page();
  return 0;
}
